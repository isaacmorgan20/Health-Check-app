import React from 'react'

const Button = (Text) => {
  return (
    <section>
        <button className='text-white rounded-lg bg-blue-800'>{Text}</button>
    </section>
  )
}

export default Button